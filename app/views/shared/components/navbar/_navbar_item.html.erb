<% label ||= "" %>
<% href ||= nil %>
<% active ||= false %>
<% dropdown ||= false %>
<% content_id ||= nil %>
<% align ||= "center" %>
<% unique_id ||= SecureRandom.hex(4) %>

<%
  base_classes = "flex items-center gap-1 rounded-md px-3 py-2 text-sm font-medium no-underline select-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-stone-600"
  state_classes = active ? "text-stone-900 bg-stone-100" : "text-stone-600 hover:text-stone-900 hover:bg-stone-100"
  item_classes = "#{base_classes} #{state_classes}"
%>

<li>
  <% if dropdown %>
    <button type="button"
            data-navbar-target="trigger"
            data-content-id="<%= content_id %>"
            aria-controls="<%= content_id %>"
            aria-expanded="false"
            data-align="<%= align %>"
            data-action="click->navbar#toggleMenu pointerenter->navbar#handlePointerEnter pointermove->navbar#handlePointerMove pointerleave->navbar#handlePointerLeave keydown->navbar#handleTriggerKeydown"
            data-state="closed"
            class="<%= item_classes %> cursor-pointer group">
      <%= label %>
      <svg xmlns="http://www.w3.org/2000/svg" class="size-2.5 transition-transform duration-200 group-data-[state=open]:rotate-180" width="12" height="12" viewBox="0 0 12 12"><g fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" stroke="currentColor"><polyline points="1.75 4.25 6 8.5 10.25 4.25"></polyline></g></svg>
    </button>
  <% else %>
    <a href="<%= href %>" class="<%= item_classes %>"><%= label %></a>
  <% end %>
</li>
